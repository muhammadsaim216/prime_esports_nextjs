import type { Metadata } from "next";
export const metadata: Metadata = { title: "Scrims & Streams", description: "Book scrim slots, watch live streams, and compete against Prime Esports rosters." };
export default function ScrimsLayout({ children }: { children: React.ReactNode }) { return children; }
