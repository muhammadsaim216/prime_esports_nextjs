import type { Metadata } from "next";
export const metadata: Metadata = { title: "Teams", description: "Meet the elite competitive rosters representing Prime Esports across multiple titles and divisions." };
export default function TeamsLayout({ children }: { children: React.ReactNode }) { return children; }