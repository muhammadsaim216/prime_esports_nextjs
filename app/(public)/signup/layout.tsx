import type { Metadata } from "next";
export const metadata: Metadata = { title: "Join Prime", description: "Create a Prime Esports account to apply for tryouts, join scrims, and follow your favorite teams." };
export default function SignupLayout({ children }: { children: React.ReactNode }) { return children; }
