import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tryouts & Recruitment", description: "Apply for open tryouts and join a Prime Esports professional roster." };
export default function TryoutsLayout({ children }: { children: React.ReactNode }) { return children; }
